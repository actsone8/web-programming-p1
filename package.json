{
  "name": "pbg-p2",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "echo \"TODO build project into dist folder for production\" && exit 1",
    "start:dev": "cross-env DEBUG=pbg-p2:* nodemon ./src/api/server"
  },
  "dependencies": {
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "morgan": "~1.9.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.20"
  }
}
